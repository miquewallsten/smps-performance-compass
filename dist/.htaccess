# Hostinger .htaccess for Node.js app with PHP webhook support
PassengerEnabled on
PassengerAppType node
PassengerStartupFile server.js

# Let PHP files be served directly by Apache/PHP
<FilesMatch "\.php$">
    SetHandler application/x-httpd-php
    PassengerEnabled off
</FilesMatch>
